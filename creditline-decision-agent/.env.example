# ── Anthropic ───────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=
AGENT_MODEL=claude-opus-4-8

# ── Environment DB (mimic_creditline) — the agent reads/writes via the MCP ───
# The MCP server connects with the LEAST-PRIVILEGE agent role.
MIMIC_DB_DSN=postgresql://creditline_agent:agent_demo_pw@localhost:5433/mimic_creditline

# Admin DSN — used ONLY for seeding/initialisation, never handed to the agent.
ADMIN_DB_HOST=localhost
ADMIN_DB_PORT=5433
ADMIN_DB_USER=postgres
ADMIN_DB_PASSWORD=postgres

# Mimic (@mimicai/cli) seeds the simulated world through this admin connection
# (it needs TRUNCATE rights for seedStrategy=truncate-and-insert). Never the
# agent role. Points at the same mimic_creditline database.
DATABASE_URL=postgresql://postgres:postgres@localhost:5433/mimic_creditline

# How long creditline_request_approval blocks waiting for a human, in seconds.
APPROVAL_TIMEOUT_SECONDS=900
APPROVAL_POLL_SECONDS=2
