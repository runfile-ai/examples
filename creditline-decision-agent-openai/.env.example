# ── OpenAI ───────────────────────────────────────────────────────────────────
# Required only to run the live agent (agent/main.py). The deterministic demo
# and the MCP/DB layers do not need a key.
OPENAI_API_KEY=
AGENT_MODEL=gpt-4.1

# ── Shared environment DB (mimic_creditline) ─────────────────────────────────
# The same database the Claude version uses. The MCP server connects with the
# least-privilege agent role.
MIMIC_DB_DSN=postgresql://creditline_agent:agent_demo_pw@localhost:5433/mimic_creditline

# Admin DSN — seeding/HITL resolution only, never handed to the agent.
ADMIN_DB_HOST=localhost
ADMIN_DB_PORT=5433
ADMIN_DB_USER=postgres
ADMIN_DB_PASSWORD=postgres

# How long creditline_request_approval blocks waiting for a human, in seconds.
APPROVAL_TIMEOUT_SECONDS=900
APPROVAL_POLL_SECONDS=2
